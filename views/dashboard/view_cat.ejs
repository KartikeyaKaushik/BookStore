<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>dashboard</title>
    <link rel="stylesheet" href="/css/registered_users.css" type="text/css" />
  </head>
  <body class="font">
    <!-- #################### header section ###################### -->
    <%- include('dash_header.ejs') %>
    <!-- ##################### header section ends ################## -->

    <div class="container-fluid">
      <div class="row">
        <!-- ################## left section #################### -->
        <div class="col-md-3 heightt p-0 bg-warning responsive">
          <%- include('left.ejs') %>
        </div>
        <!-- ################## left section ends #################### -->
        <div class="col-md-9 table-responsive-sm offset-md-3">
          <% if(data.length>0) { %>
          <table class="table">
            <tr>
              <th>S.No</th>
              <th>Category</th>
              <th>Action</th>
            </tr>
            <% for(var i=0; i<data.length; i++) { %>
            <tr>
              <td><%=i %></td>
              <td><%=data[i].category_name %></td>
              <td>
                <a href="/edit_cat/<%=data[i]._id %>" type="button" class="btn-sm btn btn-success">
                  Edit
                </a>
                <a href="/remove_cat/<%=data[i]._id %>" type="button" class="btn-sm btn btn-danger">
                  Remove
                </a>
              </td>
            </tr>
            <% } %>
          </table>
          <% } else{%>
             No Record Found 
             <% } %>
        </div>
      </div>
    </div>
    <!-- ######################### footer section ###################### -->

    <!-- ####################### footer section ends ####################### -->
  </body>
</html>
